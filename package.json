{
  "name": "botXan",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter bot-api dev",
    "start": "pnpm --filter bot-api start",
    "prisma:generate": "pnpm --filter db prisma:generate"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^17.3.1",
    "react-date-range": "^2.0.1"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
